spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/zuoyuip/config-repo.git
          username: zuoyuip@foxmail.com
          password: 1314lang1314
#          basedir: /Users/admin/code/myProjects/java/imooc/SpringCloud_Sell/config/basedir
  rabbitmq:
    host: localhost
    port: 5672
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"